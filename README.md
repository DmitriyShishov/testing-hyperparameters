На основе исходного датасета необходимо протестровать подходы к подбору гиперпараметров модели классификации.

Подходы к тестированию:
1) Базовая модель Catboost
2) GridSearch
3) RandomSearch
4) HyperOpt
